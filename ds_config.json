{
  "gradient_accumulation_steps": 1,
  "train_micro_batch_size_per_gpu": 9,
  "fp16": {
    "enabled": true
  },
  "zero_optimization": {
    "stage": 2
  },
  "checkpoint": {
    "save_dir": "./checkpoints",
    "load_dir": "./checkpoints",
    "checkpoint_steps": 1000
  },
  "flops_profiler": {
    "enabled": true,
    "profile_step": 100,
    "module_depth": -1,
    "top_modules": 3,
    "detailed": true,
    "output_file": "profiles/flops_profile_rank${RANK}.txt"
  }
}
